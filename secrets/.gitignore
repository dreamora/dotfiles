# secrets/.gitignore
# Only allow encrypted SOPS files and the SOPS config to be committed.
# Plaintext files are NEVER committed.

# Allow encrypted sops files and config
!*.sops.*
!.sops.yaml
!.gitignore
!.gitkeep

# Block everything else (catches accidental plaintext commits)
*
!*/
